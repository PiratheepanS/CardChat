
  # Generate Design

  This is a code bundle for Generate Design. The original project is available at https://www.figma.com/design/ObkbworOGWUm0AD0Q377Pa/Generate-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  